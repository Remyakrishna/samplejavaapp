trigger:
- main

pool:
  name: default
  demands:
  - agent.name -equals devtest

steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        # Write your commands here
        ls
        echo 'Hello world'

